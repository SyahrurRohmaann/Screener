export default function DataQuality({complete,dataAsOf,errors=[]}:{complete:boolean;dataAsOf?:number;errors?:string[]}){return <section className="rankPanel quality"><div><span className="rankLabel">DATA QUALITY</span><h2 className={complete?"good":"bad"}>{complete?"16 / 16 COMPLETE":"INCOMPLETE"}</h2></div><div><span className="rankLabel">DATA AS OF</span><b>{dataAsOf?new Date(dataAsOf).toISOString().replace("T"," ").slice(0,16)+" UTC":"—"}</b></div>{errors.length>0&&<p className="bad">{errors.join(" · ")}</p>}</section>}
