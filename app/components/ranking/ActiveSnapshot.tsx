export default function ActiveSnapshot({snapshot}:{snapshot?:any}){return <section className="rankPanel snapshotPanel"><span className="rankLabel">SNAPSHOT AKTIF · IMMUTABLE</span>{snapshot?<><h2>{snapshot.key}</h2><div className="topCoins">{snapshot.top4.map((x:string)=><b key={x}>{x}</b>)}</div><p className="rankNote">Dibentuk {new Date(snapshot.formedAt).toLocaleString("id-ID",{timeZone:"UTC"})} UTC · exit {new Date(snapshot.exitAt).toLocaleString("id-ID",{timeZone:"UTC"})} UTC</p></>:<><h2>Belum ada snapshot aktif</h2><p className="rankNote">Preview live tidak dihitung sebagai bukti. Snapshot hanya bisa dibentuk Senin 08:00–08:30 UTC dengan data 16/16.</p></>}</section>}
