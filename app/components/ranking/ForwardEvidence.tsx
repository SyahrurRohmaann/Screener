export default function ForwardEvidence({stats,rows=[]}:{stats:any;rows?:any[]}){return <section className="rankPanel evidence"><div className="panelTitle"><div><span className="rankLabel">FORWARD EVIDENCE</span><h2>{stats?.label??"PENGUMPULAN DATA"}</h2></div><strong>{stats?.resolvedWeeks??0}<small> MINGGU</small></strong></div><div className="evidenceGrid"><div><span>PAPER EQUITY</span><b>{(stats?.strategyEquity??100).toFixed(2)}</b></div><div><span>BENCHMARK 16</span><b>{(stats?.benchmarkEquity??100).toFixed(2)}</b></div><div><span>CUM. EXCESS</span><b>{(stats?.cumulativeExcessPct??0).toFixed(2)}%</b></div><div><span>MEAN / MINGGU</span><b>{(stats?.weeklyMeanExcessPct??0).toFixed(2)}%</b></div></div><div className="timeline">{rows.length?rows.slice().reverse().map(x=><div key={x.key}><span className={`state ${x.status.toLowerCase()}`}>{x.status}</span><b>{x.key}</b><em>{x.excessReturnPct==null?"—":`${x.excessReturnPct.toFixed(2)}% excess`}</em></div>):<p className="rankNote">Timeline kosong — eksperimen belum mengklaim hasil.</p>}</div></section>}
