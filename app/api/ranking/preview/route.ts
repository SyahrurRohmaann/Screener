import {NextResponse} from "next/server";import {preview} from "../../../lib/ranking-lifecycle";import {guard} from "../../../lib/session";export const dynamic="force-dynamic";export async function GET(){const denied=await guard();if(denied)return denied;try{return NextResponse.json(await preview(),{headers:{"Cache-Control":"no-store"}})}catch(e){return NextResponse.json({mode:"PREVIEW_LIVE",complete:false,error:e instanceof Error?e.message:String(e)},{status:503})}}
