import {NextResponse} from "next/server";import {history} from "../../../lib/ranking-lifecycle";export const dynamic="force-dynamic";export async function GET(){try{return NextResponse.json(await history(),{headers:{"Cache-Control":"no-store"}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:500})}}
