import {NextResponse} from "next/server";import {formSnapshot,requestAllowed} from "../../../lib/ranking-lifecycle";export const dynamic="force-dynamic";export async function POST(req:Request){if(!requestAllowed(req))return NextResponse.json({error:"Forbidden: same-origin request and a configured operator bearer token are both required"},{status:403});try{return NextResponse.json(await formSnapshot())}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:409})}}
