import {NextResponse} from "next/server";import {formSnapshot,requestAllowed} from "../../../lib/ranking-lifecycle";import {guard} from "../../../lib/session";export const dynamic="force-dynamic";export async function POST(req:Request){const denied=await guard();if(denied)return denied;if(!requestAllowed(req))return NextResponse.json({error:"Forbidden: same-origin request and a configured operator bearer token are both required"},{status:403});try{return NextResponse.json(await formSnapshot())}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:409})}}
