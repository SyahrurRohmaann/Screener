import {NextResponse} from "next/server";import {formSnapshot,requestAllowed} from "../../../lib/ranking-lifecycle";export const dynamic="force-dynamic";export async function POST(req:Request){if(!requestAllowed(req))return NextResponse.json({error:"Forbidden: same-origin required; bearer token also required when configured"},{status:403});try{return NextResponse.json(await formSnapshot())}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:409})}}
